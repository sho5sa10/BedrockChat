# Changelog

## v1.6.3 — 2026-08-19

### 修正
- 起動時、サーバーが listen を始める前にブラウザを開いていたため、初回表示がエラーページになることがあった。`start.ps1` / `start.sh` のどちらも、127.0.0.1:PORT に接続できるまで待ってから開くようにした。起動ログは正常に出るのに画面がエラーになるため、サーバーが落ちているように見えて紛らわしかった
  - `node server.js` は前景のままにして、待ち役だけをバックグラウンドに逃がしている（`start.ps1` は `Start-Job` + `TcpClient`、`start.sh` は `exec` 前にバックグラウンド化したサブシェル + `node` のポーリング）。node をバックグラウンドに回すとコンソールとの親子関係が変わり、`start.ps1` が既に対処している「ウィンドウを×で閉じると `node.exe` が孤立する」挙動に影響しうるため
  - 30秒待って listen しなければブラウザは開かない。開いてもエラーページになるだけで、原因はコンソールに出ている

### 追加
- サイドバーの製品名の横にバージョンを表示（`Claude Desk  ver.1.6.3`）。数字は `package.json` を唯一の正本にして `/api/config` 経由で渡す（画面側に書くとリリース時に片方だけ上げ忘れる）
- `ALLOW_CROSS_REGION_INFERENCE` — 東京リージョンのリージョンロックを解除するオプトイン（`start.local.json` に書く任意項目）。**既定は解除しない**
  - 背景: 最新モデル（Opus 5 / Sonnet 5 / Fable 5）は東京では `global.*` 推論プロファイルとしてのみ提供される。プレフィックスなしの `anthropic.claude-opus-5` は一覧上は存在するが `inferenceTypesSupported` が `INFERENCE_PROFILE` のみで `ON_DEMAND` を含まないため直接呼べない。つまりロックを掛けたままでは、`jp.anthropic.*` が追随するまで新しいモデルを一切使えない（東京で `jp.*` が存在するのは haiku-4-5 / sonnet-4-5 / sonnet-4-6 / opus-4-7 / opus-4-8 のみ）
  - 既定を変えないのは、このアプリを同僚にも配る前提で、データ所在地の要件がある環境の保護を黙って外すことになるため。解除は各環境が明示的に選ぶ
  - 解除しても黙って外れないように、サイドバー下部の表示が「東京限定」→「東京外へルーティング可」に変わり（ツールチップで理由も出る）、起動時のコンソールにも1行出る
  - ロックが有効なときは、最新モデルが一覧に無い理由と解除方法を起動時に案内する。以前は黙って一覧から消えるだけで、モデル自体が非対応だと誤解されうる状態だった

## v1.6.2 — 2026-08-19

v1.6.0 で入れた「Code経路がBedrock経由か」の警告が、**正しく設定してある環境で誤って出ていた**問題の修正。

### 修正
- Bedrock経由かどうかの判定が `process.env.CLAUDE_CODE_USE_BEDROCK` しか見ていなかった。しかし `claude` CLI の推奨される設定方法は `settings.json` の `env` ブロックで、`start.ps1` / `start.sh` はそれをサーバープロセスの環境変数には読み込まない。結果として、**settings.json で正しくBedrock経由に設定している環境に対して「Anthropicに直接通信している可能性があります」と警告していた**（素のPowerShellから起動すると必ず出る。Claude Codeが開いたターミナルから起動すると出ない、という再現性のなさもこれが原因）。狼少年になる警告は無視されるようになるため、CLI自身が読むのと同じ設定ファイルを読むようにした
- 判定を `code-agent/cli-config.js` の `resolveBedrockRouting()` に切り出し、CLIの解決順に合わせて探索する: 企業ポリシー `managed-settings.json` → Repository の `.claude/settings.local.json` → `.claude/settings.json` → ユーザーの `~/.claude/settings.json`（`CLAUDE_CONFIG_DIR` 対応）→ 環境変数。設定ファイルを環境変数より先に見るのは、CLIが設定の `env` を継承した環境変数の上に適用するため。上位で明示的に無効化されていれば下位で再有効化しない。設定ファイル名・`CLAUDE_CONFIG_DIR`・managed-settings の3プラットフォームのパスは、いずれもインストール済みCLIバイナリ内の文字列を確認して決めた（記憶や推測ではない）
- 警告の文言を「Bedrock経由に設定されていません」から「Bedrock経由かどうか確認できませんでした」に変更。ラッパースクリプトやWindowsのレジストリ経由のポリシーなど、ここから見えない設定手段が残るため、断定は誤り。設定ファイルで明示的に無効化されている場合だけは断定した文言を出す。読み取り専用のローカルファイル参照のみで、通信先は増えていない。ハードブロックしない方針も変えていない
- `package-lock.json` の `version` が 1.5.0 のまま取り残されていたのを同期（v1.6.0 / v1.6.1 での bump 漏れ）

### 追加
- `test/cli-config.test.js` — 12件。誤検知の再発防止（環境変数なし + settings.json ありで確認できること）、探索順、明示的な無効化、BOM付きJSON、壊れたJSONでクラッシュしないこと等。企業ポリシーのパスはテスト用に差し替え可能にしてあり、実際にポリシーが導入されたPCでもテスト結果が変わらない

## v1.6.1 — 2026-08-19

内部構造のみの変更。機能追加・修正はなし。

### 変更
- `public/index.html`（2600行超）から `<style>` を `public/styles.css` に、`<script>` を `public/app.js` に分離。ビルドツールは追加せず、`<link>`/`<script src>` で読み込むだけ（`express.static` が `public/` をそのまま配信する構成は変更なし）。テーマのFOUC防止用の小さな即時実行スクリプトのみ、初回描画前に間に合わせる必要があるためHTML側に残している

## v1.6.0 — 2026-08-19

macOS/Linux対応と、Code経路の実際の通信先が見えていなかった点の是正が中心。

### 追加
- macOS/Linux向けの起動スクリプト `start.sh` を追加。`start.ps1` と同じ対話式セットアップ（リージョン・プロファイル・プロキシ・CA証明書・ポート）を持ち、`start.local.json` を共有する
- **Code経路のBedrockルーティング警告**: このアプリは `claude` CLIをサブプロセスとして起動するだけで、CLI自体がBedrock経由かAnthropicへの直接通信かは制御できない。`CLAUDE_CODE_USE_BEDROCK` 環境変数が未設定のまま Codeタブに切り替えると、画面上部に警告バナーを表示するようにした（社内ポリシーでBedrock経由のみ許可されている環境向け）
- Claude Code CLIが実際に使用したモデルを、ターン実行後にヘッダーバッジ（`🛠 Claude Code ・ <Repository> ・ <Mode> ・ <モデル>`）に表示するようにした（CLIの `started` イベントから取得。今までは捨てられていた）
- GitHubリポジトリに Description と Topics（`claude` `anthropic` `aws-bedrock` `claude-code` `local-first` `windows` `enterprise` `chat-application`）を設定
- `docs/` の手描きモックアップ3枚を、実画面のスクリーンショットに差し替え（個人が特定される情報は加工済み）

## v1.5.0 — 2026-08-19

プロダクト名を **Claude Desk** に変更。あわせて、実態と食い違っていたドキュメントの整理と、git操作まわりの安全性の再点検を行った。機能の追加・削除はない。

### 変更（リネーム）
- プロダクト名を「Claude Code Chat on AWS Bedrock」から **「Claude Desk」**（サブタイトル: Chat & Code on Amazon Bedrock）に変更。名前が「Claude Code のチャット」と読めてしまい、中心機能である Bedrock 直接チャットが伝わらなかったため
- 表示名を変更した箇所: README、`<title>`、サイドバーのブランド表示、`start.ps1` の起動バナーと初回セットアップ・認証エラーの文言、サーバー起動時のコンソール出力、`package.json` の `name`（`bedrock-chat` → `claude-desk`）と `description`、`.claude/launch.json`、`docs/mockup.svg`
- GitHub のリポジトリも `Claude-Code-Chat_on_aws_bedrock` → `Claude-Desk` に移動。旧URLは GitHub のリダイレクトで引き続きアクセスできるため、既にクローン済みの環境はそのまま動く（`git remote set-url` は任意）。v1.4.0 以前のリリース資産のファイル名 (`bedrock-chat-vX.X.X.zip`) は当時のまま
- `start.local.json` の形式は変更なし（そのまま引き継がれる）

### 追加（安全性）
- ワークフローの Commit が、実行の直前に「本当にこのワークフローの隔離ブランチがチェックアウトされているか」を検証するようになった。従来は Diff 確認から Commit までの間に別のターミナルで手動でブランチを切り替えていると、`git add -A` が無関係な変更まで巻き込んで別ブランチに commit しうる状態だった（v1.3.1 で「破棄」側に入れた検証と対称になった）
- Diff 表示が、新規（untracked）ファイルと、Claude Code が自分で `git add` した変更も含むようになった。従来は `git diff` だけを見ていたため、**人間が承認した Diff に出ていないファイルが commit される**（`git add -A` は拾う）ことがありえた。新規ファイルは内容も差分として表示される

### 追加（テスト）
- 回帰テストを6件追加
  - Commit が隔離ブランチ以外では拒否され、かつ何もステージしないこと（`test/git-adapter.test.js`）
  - 「破棄」が隔離ブランチ以外では checkout / clean を一切実行せず、ユーザー自身の変更が生き残ること（同）
  - 「破棄」の `git clean -fd` が `.gitignore` 対象のファイル（`start.local.json` / `data/`）を消さないこと（同）
  - Diff が新規ファイル・staged 済みの変更を取りこぼさず、`.gitignore` 対象は含めないこと（`test/git-info.test.js`、新規ファイル）
  - テスト実行のタイムアウト時に、cmd.exe → npm.cmd → node のプロセスツリー全体が終了すること（`test/test-runner.test.js`）
  - イベントログが上限に達したあとも `turnStartIndex` が現在のターンの先頭を指し続けること＝ブラウザ再接続時に応答が欠落しないこと（`test/sessions.test.js`）

### 変更（ドキュメント）
- README を再構成。「これは何か」（claude.ai / Claude Desktop が使えない環境向け）→「2つのモード」→「アーキテクチャ」→「課金の違い」→「セキュリティ」の順に読めるようにした
- アーキテクチャ図を実態に合わせて差し替え。従来は「ブラウザ → サーバー → Bedrock」の一本道しか書かれておらず、Code タブがローカルの `claude` CLI を起動していることが図から読み取れなかった。ホーム経路（Bedrock直）と Code経路（Claude Code CLI）を分けて図示している
- 課金の違いを独立した節にした。ホームは AWS の Bedrock 利用料、Code は Claude Code の利用料で、請求先が別であることを明記
- Roadmap を実態に合わせて更新。すでに実装済みだった「プロンプトテンプレート」「会話履歴の検索」を削除し、未実装のもの（会話の分岐、Artifactsプレビューの拡張など）だけを残した
- 「うまくいかないとき」に、隔離ブランチ関連の2つの拒否メッセージ（未コミットの変更がある / ブランチが切り替わっている）の対処を追加

### 変更（UI）
- Code スレッドであることが一目で分かるように、ヘッダーのバッジを `🛠 Claude Code ・ <Repository> ・ <Mode>` にし、「Claude Codeと会話する」ボタンと同じ青で表示するようにした（長いリポジトリパスでヘッダーが崩れないよう省略表示にしている）
- ホーム / Code のスレッドを開いた直後の空画面、および Claude Codeチャット開始・実装依頼の両モーダルに、どちらの経路でどちらに課金されるかの注記を追加

## v1.4.0 — 2026-08-17

社内利用のために手元で作っていた機能をまとめて取り込み。

### 追加
- zipファイルの添付。展開して中身の一覧から必要なファイルだけを選んで添付できる（対応形式のみ表示）。zipの中身はサーバー側でもディスクに書かず、メモリ上に10分だけ保持する
- Artifactsプレビュー（HTML / SVG / XML のコードブロック限定）。コードブロックの「プレビュー」ボタンでその場に描画する。描画は `sandbox="allow-scripts"` のiframe内で行い、さらにCSPで外部通信をすべて遮断するため、「通信先はBedrockのみ」の方針は維持される。React実行やコード実行（Analysisツール相当）は未対応
- コンテキスト使用量バー。直近のやり取りのトークン数を、Claude系の標準コンテキスト長（200Kトークン）に対する割合として画面上部に表示する（モデルごとの実際の上限はAPIから取得できないため固定値による近似）
- 会話タイトルの自動生成。最初の一往復から短いタイトルをClaudeに付けさせる（従来は本文の先頭34文字）
- 応答スタイルのプリセット（簡潔 / 正式 / フレンドリー / 技術的）と、全会話に共通で渡す「メモ」欄。どちらもシステムプロンプトに結合されるだけの軽量実装で、サーバー側には保持しない
- 東京リージョンでの利用時に、東京以外へ処理がルーティングされるクロスリージョン推論プロファイル（`global.*` / `apac.*` / `us.*`）を拒否するリージョンロック。モデル一覧・チャット・タイトル生成のすべてに適用され、サイドバー下部に「東京限定」と表示される
- `start.ps1` に、ポートを掴んだまま孤立した `node.exe` の自動終了と、起動前のAWS認証チェック（失効時は再ログインを試行）を追加。再ログインに使うコマンドは `start.local.json` の `LOGIN_COMMAND` で差し替えられる（既定 `aws sso login`）

### 修正
- ドキュメント: 会話履歴について「サーバー側には残らない」と書かれていた箇所を、v1.2.0でサーバー側バックアップ（`data/history.json`）を追加した実態に合わせて修正

## v1.3.1 — 2026-08-17

コードレビューで見つかったバグをまとめて修正。

### 修正（データ消失系）
- サーバー履歴バックアップの読み込みに失敗した状態でチャットを続けると、復旧可能だったはずの既存バックアップファイルが空データで上書きされてしまう不具合を修正。読み込みが確認できるまでサーバーへの書き込みを行わないように変更
- Claude Codeワークフローの「破棄」が、実際に隔離ブランチ上にいるか確認せずに `git checkout --` / `git clean -fd` を実行していた不具合を修正。手動でブランチを切り替えていた場合などに無関係な変更を消しうる状態だった

### 修正（機能不具合）
- 長時間のClaude Codeセッションでイベント件数が上限に達すると、ブラウザ再接続時に直近ターンの応答が一部欠落する不具合を修正
- Windowsでテスト実行がタイムアウトした際、実際のテストプロセスが終了せず残り続ける不具合を修正
- Codeタブでのクイックスタート中に連続送信すると、スレッドとセッションが二重に作られる不具合を修正
- 履歴のサーバー保存に失敗しても無言で握りつぶされていたのを、画面上に警告表示するように変更
- `/api/chat`のAWSエラーにも、`/api/models`と同じ分かりやすいエラーメッセージ変換を適用
- Codeタブでスレッド未選択のまま「Markdownで保存」を押すと、勝手にホームタブへ新規チャットが作られる不具合を修正。同根の不具合（バックグラウンドの応答完了イベントなどが意図せず新規チャットを作ってしまう）を計14箇所で修正
- 設定バナーの閉じるボタンが、ダークモードでホバーしても見えなかった不具合を修正
- AWS設定エラーと履歴バックアップエラーが同時に発生すると、後着の警告が先着の警告を消してしまっていたのを、両方同時に表示するように変更

## v1.3.0 — 2026-08-17

初めて触る人でも迷わず起動できるように、オンボーディング周りを改善。

### 追加
- AWSの認証情報・権限・リージョン設定などに問題があるとき、画面上部に原因と対処法を表示する警告バナーを追加（今までは無言でモデル欄が空の入力欄に切り替わるだけだった）
- `start.ps1` を対話式に変更。初回起動時にリージョン・AWSプロファイル・プロキシ・CA証明書・ポート番号を質問し、`start.local.json`（Git管理外）に保存。次回以降はファイルの手編集なしでそのまま起動する。`.\start.ps1 -Reconfigure` で設定をやり直せる

## v1.2.0 — 2026-08-17

### 追加
- サイドバーを「ホーム」（Amazon Bedrock直接チャット）と「Code」（Claude Code）タブに分割。Claudeアプリと同様の操作感に
- サーバー側の会話履歴バックアップ（`data/history.json`）。ブラウザのlocalStorageを消しても、次回アクセス時にサーバー側から復元されるように（会社PCでキャッシュを消す運用でも履歴が失われない）
- Claude Codeとの会話（Codeタブ）で、スレッド未作成の状態でもメッセージを入力して送信するだけでチャットが開始されるように（ホームタブと同じ操作感）。Repository/Modeは前回使用したものを自動で記憶・再利用
- ユーザー発言へのホバーアクション（コピー・編集して再送信・巻き戻す）とタイムスタンプ表示
- Claudeの回答にもタイムスタンプを表示
- 応答中に押せる「中断」ボタン（Bedrock直接チャット・Claude Codeチャットの両方）

### 変更
- ホバーアクションのアイコンを絵文字から、より控えめな線画アイコンに変更
- アシスタント表示ラベルを「Claude」の文字からアイコンに変更

### 修正
- 「新しいチャット」と「Claude Codeと会話する」の作成ボタンが、選択中のタブに関わらず同時に表示されてしまう不具合を修正（`hidden`属性とCSSの`display`指定が競合していたのが原因）
- トークン使用量などのメタ情報表示が本文と同じ大きさになっていた不具合を修正（CSSの誤削除が原因）

## v1.1.2 — 2026-08-17

### 修正（v1.1.1の追加修正・こちらが本質的な原因でした）
- スレッド一覧の「⋯」メニューとチャット一括削除バーが、CSSの指定により常に画面に表示されたままになっていた不具合を修正。`hidden`属性を切り替えても、`display`を固定値で指定していたスタイルがそれを打ち消してしまっていたのが原因でした。v1.1.1はJS側の閉じるタイミングだけを直していましたが、実際の表示崩れの原因はこちらでした

## v1.1.1 — 2026-08-17

### 修正
- スレッド一覧の「⋯」メニュー（ピン留め・複製・アーカイブ）が、クリックを伴わない操作（Enterキーでの送信など）を続けていると閉じずに残ってしまう不具合を修正

## v1.1.0 — 2026-08-17

アプリ名を「Claude Code Chat on AWS Bedrock」に変更。Claude Code連携とUIを大幅に刷新。

### Claude Code連携
- 設計相談から「🛠 Claude Codeで実装する」で、Claude Codeへの一発実装依頼が可能に
- Claude Codeを継続的な会話の相手として使う「Claude Codeチャット」モード（`--resume`によるセッション継続、ツール利用のライブ表示、ブラウザ再接続対応）
- 実装は必ず承認制のワークフロー（Plan → 人間の承認 → 実装（隔離ブランチ） → テスト（検出したコマンドのみ） → Diff確認 → 人間の承認 → Commit）を経由。自動でのファイル変更・push・mergeは一切なし
- 実行中セッションの中断、トークン使用量・所要時間・概算コストの表示

### UI刷新
- ダークモード（OS設定に自動追従、明示的な切替も可能）
- ユーザー発言を右寄せの吹き出しに、Claude側はアバターなしのプレーン表示に刷新
- 回答へのホバーでコピー・再生成、長い回答の折りたたみ表示
- コードブロックに言語名表示と軽量シンタックスハイライトを追加
- 入力欄をピル型デザインに、「↓最新へ」フローティングボタンを追加
- サイドバーにチャット検索、ピン留め・アーカイブ・複製・一括削除などのスレッド管理機能
- 送信済みメッセージの編集→再送信（Bedrock直接チャットのみ）
- `/`で呼び出すプロンプトテンプレート、`?`キーでのショートカット一覧
- 応答完了時の通知音・デスクトップ通知（任意設定）

## v1.0.0 — 2026-08-02

初回リリース。

### 主な機能
- Amazon Bedrock (Converse Stream API) を使ったストリーミングチャット
- Claude Code と同じ AWS 認証情報 / プロキシ / CA証明書設定を利用
- モデル切り替え（推論プロファイルの自動取得、IDの直接入力にも対応）
- システムプロンプト・temperature・最大出力トークン・拡張思考（Thinking）の設定
- ファイル添付（画像・PDF・Office文書など）
- 許可フォルダを画面で登録し、フォルダ内のファイルを添付せずに参照
- 会話履歴のローカル保存、Markdown書き出し
- コードブロックのシンタックス風表示・コピー
