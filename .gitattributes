# start.sh must stay LF — a CRLF shebang line breaks it on macOS/Linux,
# and Windows' core.autocrlf=true would otherwise silently reintroduce
# CRLF into the working tree (though never into the stored blob).
*.sh text eol=lf
